# Audio / MIR / Music Theory discoveries — 2026-06-03

Scoring is 1–5 against relevance to **ASA** and **Harmonia**. Anything below 3 was
dropped. Dedup'd against the consolidated `discoveries/_seen.txt` (124 entries at
time of run).

Run against `routines/audio-mir.md`: **ASA is a server-side Python/FastAPI app**
doing analysis on NATIVE libraries (Essentia primary, plus librosa, torchcrepe,
Demucs; PyTorch already in the stack) and a Gemini/LLM layer; it already emits a
large analysis JSON (spectral, chroma/HPCP, key, chords, genre, EBU R128, BPM/beats,
structure, stems, melody). Native C/C++/Rust/Python and PyTorch models are
first-class — no credit for browser/WASM/Essentia.js. **Harmonia** is a React
chord-progression / reharmonization tool on Tonal.js.

## ASA-relevant

### [OpenMOSS/MOSS-Audio](https://github.com/OpenMOSS/MOSS-Audio) — **4**
Open-source audio-understanding foundation model (4B/8B, Instruct + Thinking
variants, weights on HF/ModelScope, run via `infer.py` / Gradio / SGLang) covering
music understanding, audio captioning, time-aware QA and multi-hop reasoning over
audio. It's the broader sibling of MOSS-Music (logged 05-21) and the same story for
ASA's Gemini layer: a self-hostable model that produces exactly the kind of
LLM-interpreted, timestamp-aligned analysis ASA currently outsources to Gemini, so
it's a serious reference (or partial in-house replacement) for that layer.

### [SUC-DriverOld/MSST-WebUI](https://github.com/SUC-DriverOld/MSST-WebUI) — **4**
WebUI bundling Music-Source-Separation-Training models plus UVR, with ensemble
modes and a built-in SOME MIDI extractor (Python, 1.2k★, 14 releases). It's the
canonical hub for the current best-in-class separators (MSST/VR models, BS-RoFormer
et al.) that beat plain Demucs, so it's the obvious place to shop for an upgrade to
ASA's stems stage — the inference is scriptable even though the front end is a
PySide6 desktop app rather than a server backend.

### [gluon/AbletonLive12_MIDIRemoteScripts](https://github.com/gluon/AbletonLive12_MIDIRemoteScripts) — **3**
Unofficial collection of the Ableton Live 12.4 MIDI Remote Scripts (Python) with an
accompanying Live Object Model documentation site spanning Live 9–12. ASA's Phase 2
emits Live 12 device/parameter/value recommendations, so this is a useful reference
for real device-control mappings and parameter-naming patterns — but it's a
controller-script collection, not a complete built-in-device API, so treat it as a
naming/structure crib rather than ground truth.

### [Evilander/logic2ableton](https://github.com/Evilander/logic2ableton) — **3**
Reverse-engineered converter that reads Logic projects and reconstructs Ableton
`.als` sets (audio placement, tempo, time signatures, MIDI), and the reverse, with
VST3 plugin suggestions emitted in the conversion report (Python + a TS desktop app,
PyPI-packaged, v2.0.3). For ASA the value is the working `.als`-writing code path:
if ASA ever wants to emit a starter Live set or device chain from its
recommendations, this is a concrete model for producing a valid `.als`.

### [M-Igashi/headroom](https://github.com/M-Igashi/headroom) — **3**
Rust CLI that measures integrated LUFS and true peak and applies limiter-free gain
to a uniform true-peak ceiling, with a companion Camelot-key→BPM playlist sorter
(v2.0.0, May 2026). Native and clean, it overlaps ASA's existing EBU R128
measurement, so its real interest is the true-peak-ceiling normalization logic ASA's
mastering-recommendation layer could cite — borderline, kept as a native reference
rather than a new capability.

## Harmonia-relevant

### [Metacreation-Lab/MIDI-GPT](https://github.com/Metacreation-Lab/MIDI-GPT) — **3**
GPT-2 transformer for controllable multitrack symbolic generation that infills
missing bars while respecting existing tracks and conditions on attributes like note
density and polyphony, shipping pip-installable inference with pretrained models and
HTTP/OSC servers. For Harmonia it's a generation/infilling engine to suggest or
complete progressions under constraints — but it's MIDI/PyTorch, not Tonal.js, so
it's a technique to borrow (or a sidecar service), not code to lift into the React app.

## Tangential but interesting

### [BinWang28/audio-ai-hub](https://github.com/BinWang28/audio-ai-hub) — **3**
Curated, weekly-refreshed hub of audio-AI papers, open models, benchmarks and
datasets across audio LLMs, music understanding/generation and speech (121 entries,
11 categories). Not code to fork, but the most efficient single scouting surface for
ASA's Gemini-layer roadmap — it's where MOSS-Audio-class models and music-understanding
benchmarks surface first.

---

Dropped (off-stack, thin, redundant, or excluded; surfaced this run and added to
`_seen.txt` so they don't resurface):

- **Yikai-Liao/symusic** — fast C++/Python symbolic-MIDI toolkit, but it's
  MIDI-processing not harmonic analysis, and it's the wrong stack (not Tonal.js) for
  Harmonia and adds nothing over ASA's existing pipeline.
- **olilarkin/paulstretch-for-live** — Ableton extension embedding Paulstretch; a
  timestretch FX, not analysis or theory.
- **federico-pepe/ableton-live-extensions** — experiments with the Ableton Extensions
  SDK; too thin to be a reference.
- **XIAODUOLU/Midra**, **Chz1Y/Steerable-music-transformer** — niche prompt-to-MIDI /
  controllable-generation research repos; off-target for ASA analysis and Harmonia
  reharmonization.
- **SNU-LIST/chi-separation** — magnetic-susceptibility "source separation" for MRI;
  surfaced on the keyword, off-domain.
- **Excluded (keyword-spam / pirated):** `e531538342049/FL-Product-Version-Full-2`,
  `Griffinfloexalt/Suno-AI-Pro-cracked` — pirated-software listings, not real repos.
</content>
</invoke>
