# Audio / MIR / Music Theory discoveries — 2026-05-21

Scoring is 1–5 against relevance to **ASA** and **Harmonia**. Anything
below 3 was dropped. Dedup'd against the consolidated `discoveries/_seen.txt`
(86 entries at time of run).

Run against the corrected routine spec (`routines/audio-mir.md`): **ASA is
a server-side Python/FastAPI app** doing analysis on NATIVE libraries
(Essentia primary, plus librosa, torchcrepe, Demucs; PyTorch already in
the stack) and a Gemini/LLM layer; it already emits a large analysis JSON
(spectral, chroma/HPCP, key, chords, genre, EBU R128, BPM/beats,
structure, stems, melody). Native C/C++/Rust/Python and PyTorch models are
first-class — no credit for browser/WASM/Essentia.js. **Harmonia** is a
React chord-progression / reharmonization tool on Tonal.js.

> **Correction (2026-05-22):** the Harmonia description above is wrong. Harmonia is **not** a
> React/Tonal.js GitHub project — it's a real but **unpublished, single-file, dependency-free
> vanilla-JS** chord/reharmonization tool (`github.com/slittycode/harmonia` → 404). The
> "Harmonia-relevant" / stack-fit scoring below was judged against an inaccurate target; those repos
> remain valid as **conceptual references only**. Corrected framing: `routines/audio-mir.md`,
> `baseline/music-asa/`. This record is left intact on purpose.

> Supersedes the earlier same-day pass: that one ran against a stale,
> pre-reorg `_seen.txt` and re-surfaced three repos (music21-mcp-server,
> MusicalKeyCNN, bliss-rs) that the 05-17/18/19 sweeps had already logged.
> This rerun is against the full consolidated list; only genuinely new
> repos are below.

## ASA-relevant

### [OpenMOSS/MOSS-Music](https://github.com/OpenMOSS/MOSS-Music) — **5**
Open 8B music-understanding LMM (MOSS-Audio-Encoder + Qwen3-8B, weights
on HF/ModelScope with SGLang/Transformers/Gradio inference) that takes
raw audio and does lyrics ASR with timestamps, captioning, intro/verse/
chorus structural analysis, chord/key/tempo reasoning, and long-form
musical Q&A. This is the single most on-point new find for ASA's Gemini
layer — it's an open, self-hostable model producing exactly the
LLM-interpreted analysis ASA currently gets from Essentia + Gemini, so
it's a serious reference (or partial replacement) for that layer and its
task decomposition maps straight onto ASA's JSON.

### [geshang777/GaMMA](https://github.com/geshang777/GaMMA) — **3**
Research implementation for "joint global-temporal music understanding in
large multimodal models" — another audio-LLM aimed at reasoning over both
whole-track and time-localized musical structure. Same direction as ASA's
LLM layer and a useful second data point, but it's a paper repo rather
than a packaged model, so it's less immediately usable than MOSS-Music.

## Both

### [andreamust/consonance-ACE](https://github.com/andreamust/consonance-ACE) — **4**
Audio chord-estimation model (Conformer) that decomposes prediction into
separate root / bass / pitch-activation heads with consonance-based label
smoothing, shipping a pretrained checkpoint and inference that turns WAV
into 170-class timestamped chord `.lab` output. Directly relevant to ASA's
existing chord-detection stage as a modern, theory-informed model you can
run server-side, and its timestamped chord stream is exactly the input
Harmonia consumes — so it sits across both projects.

## Harmonia-relevant

### [sivabenepoivediamo/musicplusplus](https://github.com/sivabenepoivediamo/musicplusplus) — **3**
Header-only C++ music-theory library using vector-based representations
for chords, scales, intervals, voice leading, and reharmonization (modal
interchange, modulation), with TypeScript and Python SDKs on the roadmap.
The reharmonization/voice-leading coverage is dead-center on Harmonia's
domain — but it's C++ today, so it's an algorithm reference (or a future
dependency once the planned TS SDK lands), not a Tonal.js drop-in.

### [fpachet/continuator](https://github.com/fpachet/continuator) — **3**
François Pachet's reimplementation of the Continuator: variable-order
Markov modeling plus exact finite-chain inference to generate melodic and
chord-sequence continuations with *guaranteed* positional constraints,
real-time learning, and tiny data needs. For Harmonia it's an interesting
non-transformer approach to suggesting or completing progressions under
hard constraints (e.g. "keep these anchor chords"); it's Python/symbolic,
so it's a technique to borrow rather than code to lift.

### [comorebi-notes/rechord](https://github.com/comorebi-notes/rechord) — **3**
React + Tone.js app for writing and sharing chord progressions, still
getting commits. On Harmonia's exact stack family (React + Tone.js +
chords), so it's a usable reference for progression-entry UI and Tone.js
playback wiring — but it's a 2017 sharing app with no reharmonization
logic, so there's nothing to take on the theory side.

## Tangential but interesting

Nothing else cleared the bar. Closest near-misses in Dropped.

---

Dropped (off-stack, thin, or duplicate-of-known; surfaced this run and
added to `_seen.txt` so they don't resurface):

- **NeptuneHub/audiomuse-ai-plugin** — Jellyfin sibling of the
  already-logged AudioMuse Navidrome plugin; same librosa+ONNX+LLM sonic-
  analysis architecture, no new DSP.
- **RowanUnderwood/Synesthesia-AI-Video-Director** — audio→LLM→video tool,
  but the "audio analysis" is pydub silence detection and the LLM writes
  *video* prompts; off-domain for ASA.
- **ubisoft/ComfyUI-Chord** — ComfyUI node wrapping Ubisoft's "Chord"
  audio model; generation-side and ComfyUI-bound, off-target for both.
- **lorediggia/harmony-lab** — minimal Rust scale/chord explorer; too
  small and wrong stack for Harmonia.
- **sepandhaghighi/capo** — Python guitar-chord transposition; Tonal.js
  already does transposition for Harmonia.
- **timvancann/chordflow** — Rust chord-practice TUI; not analysis or
  theory tooling.
- **JJ110112/LiveChord** — no README/description to vet against; skipped.
- **adamstark/Gist**, **NeptuneHub/AudioMuse-AI-MusicServer**,
  **sigsep/sigsep-mus-eval** — established/known or integration-shell
  repos; nothing new next to what ASA already depends on.
- **joanroig/midi-to-scaler-chord-sets**, **ManasWolrd/WarpCore**,
  **ZaneH/piano-trainer**, **emjjkk/beat-detection** — niche, off-stack,
  or under the 20-star bar.
</content>
